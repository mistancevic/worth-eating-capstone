# Incomplete

Three of eight files. The other five were lost recovering the zip from Drive
through a text channel — the same block failed twice, identically.

**Here:** `AGENT_BLUEPRINT_GRILL.md`, `EVAL_CASES_STARTER.md`,
`SYSTEM_PROMPT_STARTER.md`.

**Missing:** `02_DESIGN.md` (the walkthrough), `AGENTS.md` (companion rules),
`DESIGN_PRD_TEMPLATE.md`, `START_HERE.md`, `NORTHSTAR_HOME_EXAMPLE.md`,
`00_HOW_TO_WORK_WITH_AI.md`, `CODEX_SETUP_GUIDE.md`.

**Fix:** attach the zip to a chat message rather than linking it. It lands on
disk and needs no re-encoding.

The start prompt is not missing — it is in `docs/design.md`, pasted from the
course page, and it carries the ten PRD fields and the grilling instruction.
